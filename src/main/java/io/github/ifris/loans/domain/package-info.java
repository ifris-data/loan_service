/**
 * JPA domain objects.
 * 
 * For now the main domain onject is the loanAccount. The same abstracts the current
 * loan listing produced by existing banking system, with all the 20-soomething columns
 * APIs that require to be light weight will need to create light-weight view models
 */
package io.github.ifris.loans.domain;
